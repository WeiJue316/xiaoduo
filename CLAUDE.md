# CLAUDE.md — xiaoduo 自动回复质量评估流水线

> 本文件是本项目的最高约束。所有开发、评估、写作都必须遵守。
> 优先级：本文件 > 用户全局指令（冲突时以本文件为准，但不得违反全局第一性原理）。

---

## 1. 项目一句话

把业务方一句模糊的需求「回复要准确、有用、语气好、不能瞎编」变成一套**可运行、可解释、可复用**的自动回复质量评估流水线，用于决定是否扩大自动回复覆盖范围（最终要能定期自动跑）。

## 2. 第一性原理：评估的本质

评估不是给 LLM 打分，而是**在回答一个业务决策**：这套自动回复到底能不能扩大上线。

- 业务方不关心"指标怎么算"，只关心**哪些回复好、哪些不好、为什么好/不好**。
- 评估报告的核心输出是**可解释的结论**，不是一堆分数。
- 任何指标如果答辩时不能讲清"为什么这么定义、为什么这么量化"，就不合格。

**从人工标注提炼出的核心判断主线（本项目的地基，不得偏离）：**
好评与否的关键 = **是否主动帮用户解决问题，而不是把责任/操作/判断推回给用户**。
「正确但没用」的回复（让用户自己看详情页、自己查订单、自己操作）在业务价值上等同差回复。

## 3. 数据与文件约定（不可违反）

| 文件 | 角色 | 约定 |
|---|---|---|
| `attachments/task3_eval_criteria.md` | 业务方原始需求 | **只读，永不修改** |
| `attachments/task3_auto_replies.json` | 被评估的 20 条输入 | **只读，永不修改** |
| `attachments/task3_human_ref.json` | 人工标注（验证用 ground truth） | **只读，永不修改**；不得把标注内容硬编码进评测逻辑来"凑分数" |

- 评测代码必须对原始输入逐条独立评分，**不读取 human_ref 参与推理**。human_ref 只用于事后退验我们的指标是否贴合人工判断。
- 新产生的数据（评分结果、报告）一律放 `outputs/`，不污染原始附件。

## 4. 评估指标（既定方向，具体量化以代码实现为准）

业务方的 4 个模糊词 → 可量化的指标。**必须 ≥ 3 个**，推荐如下，按优先级排序：

1. **有用性 / 主动协助（核心，权重最高）** — 是否直接解决问题、给出可操作路径，**是否主动"帮用户做"而非"让用户自己做"**。量化：对照 `user_question` 打分（0–5）。这是从 20 条标注中验证出的最强判别维度。
2. **准确性与不瞎编（Factual）** — 是否事实正确、不臆造；回答是否命中问题核心而非"泛泛而谈/答非所问"。量化：0–5 + 不瞎编标记。
3. **语气与同理心（Tone）** — 情绪场景（生气、害怕、抱怨）是否有足够安抚、道歉是否诚恳到位。量化：0–5。

**实现时可选增减，但必须**：(a) 说明每个指标"为什么这么量化"；(b) 说明指标间的优先级与理由；(c) 用 human_ref 验证指标与人工判断的一致性（量化一致性指标，如相关系数/命中率），并如实报告偏差。

## 5. 目录与命名规则

```
D:\Project\xiaoduo\
├── CLAUDE.md            ← 本文件
├── README.md             ← 面向读者：指标定义、评估方法、局限性、AI 工具使用
├── attachments\          ← 原始输入（只读，见 §3）
├── src\                  ← 评测流水线源码（模块化：指标计算、评分、报告生成分离）
├── scripts\              ← 可复用的运行脚本 / 自动化脚本
├── outputs\              ← 所有生成物：逐条评分、各指标分布、评估报告（git 可忽略）
└── tests\                ← 单元/验证测试（含对 human_ref 的一致性验证）
```

- 文件名/目录名 / 代码：**英文**；报告、注释、文档正文：**中文**。
- 命名清晰无歧义，不出现 `v1_v2_final` 这类命名。
- `outputs/` 若无需入库，在 `.gitignore` 中忽略（报告类可入库的除外）。

## 6. 代码与运行约束

- **模块化**：指标计算（evaluation）、评分引擎（scorer）、报告生成（reporter）分离，便于替换 LLM 为 mock、便于单测。
- **可复现**：同一份输入 + 同一配置 → 确定性或可记录的结果。评分必须记录使用的模型/版本/参数。
- **LLM 与 mock 可切换**：支持真实 LLM API 与 mock 模式，切换只改配置不改逻辑；mock 返回结构必须与真实 API 对齐。
- **不强一行、不炫技**：这是给学生自学的项目，代码可读性 > 简洁性。复杂逻辑写注释讲"为什么"。
- **不注释掉报错规避问题**，找根本原因。

## 7. 验证要求（改完必须验）

- 每次改动后运行：`python -m pytest`（或项目约定命令）确认不破坏现有功能。
- 评估方法完成后，**必须跑 human_ref 一致性验证**并把结果写进报告与 README。
- 报告里的结论（最差 case、指标分布）要与实际评测输出一致，不得编造数字。

## 8. AI 工具使用规范（如实记录）

- 本项目允许且**鼓励**使用 AI（Claude Code 等）辅助开发、写作、分析。
- **README 和报告必须如实说明 AI 工具使用情况**：用了哪些工具、在哪一步帮忙、产出了什么，不得隐瞒。
- AI 辅助产出的数值结论（评分、统计）必须能追溯到实际运行结果，不得由 AI 凭空补数字。

## 9. Git 提交约定

- commit message 用**英文**，简洁描述变更意图。
- git push 仅用于跨设备同步，**不自动执行**，等用户明确说。
- 提交时不得把密钥/token 入库；`.env` 类文件进 `.gitignore`。